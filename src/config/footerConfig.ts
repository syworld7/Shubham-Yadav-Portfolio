import { siteConfig } from './constants';

export const footerConfig = {
  title: `Frontend Developer with ${siteConfig.experienceYears} years of experience building responsive web applications using React.js, TypeScript, and modern frontend technologies.`,
};
